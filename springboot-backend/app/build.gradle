// 👉 应用 Spring Boot 插件（只有启动模块需要）
apply plugin: 'org.springframework.boot'
apply plugin: 'io.spring.dependency-management' // 自动匹配 BOM 版本

// 👉 依赖配置
dependencies {
    implementation project(':springboot-backend:shared')
    implementation project(':springboot-backend:authService')
    implementation project(':springboot-backend:projectService')
    implementation "org.springframework.boot:spring-boot-starter-webmvc:${springBootVersion}"
    implementation "org.springframework.boot:spring-boot-starter-data-jpa:${springBootVersion}"
    implementation("org.springdoc:springdoc-openapi-starter-webmvc-ui:3.0.0")
    developmentOnly "org.springframework.boot:spring-boot-devtools:${springBootVersion}"
    annotationProcessor "org.springframework.boot:spring-boot-configuration-processor:${springBootVersion}"
    runtimeOnly 'org.postgresql:postgresql'
}

// 👉 打包配置 (可选)
jar {
    enabled = true
}

bootJar{
    enabled = true
    archiveFileName = "${project.name}.jar"
}