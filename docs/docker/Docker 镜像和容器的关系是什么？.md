可以类比为面向对象编程中的类和示例
镜像：一个只读的模板，包含了运行所需的所有内容，包括代码、运行时环境、库、环境变量和配置文件。类比为类

容器：镜像的运行示例，docker会在该镜像之上创建一个可写的容器层（或称为读写层）
用来保存运行时产生的数据（日志、临时文件、用户操作等），多个容器可以同时从同一个镜像启动，彼此隔离

生命周期：镜像是静态、长期存在的；容器是动态的，可启动、停止、删除
存储：镜像是分层存储；容器基于镜像+一个读写层
创建方式：镜像通过Dockerfile构建或拉取；容器通过docker run 启动

一个镜像可被多次使用，每个容器是独立的运行实例