打标签语义化版本，避免使用latest到生产环境，或者在结合CI/CD系统在构建阶段根据git提交、分支名和语义化版本自动生成镜像标签

使用私有镜像仓库，如Nexus，支持访问控制、漏洞扫描、镜像签名等功能

镜像命名，统一命名空间，如<registry>/<team>/<service>:<tag>

使用Notary或cosign对镜像进行数字签名

## 镜像生命周期管理

通过CI/CD自动化构建与推送

集成Trivy、Clair、Aqua等工具在推送或拉取时自动扫描漏洞，高危漏洞镜像禁止部署

保留最近N个版本，删除超过X天未使用的镜像