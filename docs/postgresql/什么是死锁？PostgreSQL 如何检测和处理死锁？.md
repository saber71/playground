死锁，两个或多个事务在执行过程中都在争夺资源而互相等待的现象。
如：
事务A持有资源X的锁，请求资源Y的锁
事务B持有资源Y的锁，请求资源X的锁
此时A、B互相持有对方想要的资源同时又互相请求对方持有的资源，于是双方永远的阻塞

PG采用延迟检测+等待图算法
当一个事务尝试获取锁但被阻塞时，不会立即检查死锁
当事务阻塞时间超过阈值时，启动死锁检测
pg构建一个有向图，节点是事务id，边表示事务A等待事务B持有的锁
通过深度优先搜索检测图中是否存在环，存在即为死锁
pg会随机选择一个事务（通常是最后加入等待连的那个）向其返回错误，它自动回滚，释放所有锁，打破死锁循环
