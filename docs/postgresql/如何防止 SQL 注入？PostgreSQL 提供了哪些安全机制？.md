- 参数化查询，确保用户输入不会被解释为sql代码，避免动态拼接sql
- 动态对象名（pg不支持参数化表名、列名）应进行白名单校验
- 数据库用户权限为满足其任务的最小权限
- 对输入进行验证与转义

- pg内置转义函数
  quote_ident()安全转义sql标识符、quote_literal()安全转义字符串字面量、format()
  配合%I（标识符）和%L（字面量）
- 行级安全，允许自动过滤列数据，只能让攻击者看到授权的数据
- 严格的权限控制
- 元命令（如\copy）不经过服务器解析，防止注入