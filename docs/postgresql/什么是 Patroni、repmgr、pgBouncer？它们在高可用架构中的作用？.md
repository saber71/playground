patroni，python编写的开源模板和守护进程，负责监控数据库撞他，管理主从复制和在故障时自动或手动切换主库
通过分布式存储（如etcd）检测数据库状态
自动从剩余的从库中选举出一个新的主库，通常只需几秒到几十秒，无需人工干预

repmgr，管理pg的复制集群，简化搭建主从复制、添加新节点、重新同步备库等复杂操作，支持故障自动转移

pgBouncer，轻量级的pg连接池器，位于应用与pg之间，作为中间件存在
支持连接复用，将应用的短连接复用到这些持久连接上
限制最大连接数，防止突发流量冲垮数据库
也可以将读请求分发到备库上